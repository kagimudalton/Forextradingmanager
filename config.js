/* config.js — EDIT THIS after you deploy the backend (Render/Railway/etc).
   This is the only file you need to touch when moving between environments.

   Example: BACKEND_URL = "https://forex-manager-api.onrender.com";
   Leave as "" ONLY if the frontend is served by the same FastAPI app
   (i.e. you did NOT split frontend onto GitHub Pages). */

const BACKEND_URL = "https://forextradingmanager-backend.onrender.com";
